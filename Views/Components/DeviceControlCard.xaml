<UserControl x:Class="GD_ControlCenter_WPF.Views.Components.DeviceControlCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GD_ControlCenter_WPF.Views.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="72" />
                </Grid.ColumnDefinitions>

                <Button Command="{Binding OpenDetailCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Height="Auto" 
                        Padding="8,6" 
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch">
                    <ContentControl Content="{Binding}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                </Button>
                
                <Button Grid.Column="1"
                        Command="{Binding ToggleCommand}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Height="Auto"
                        materialDesign:ButtonAssist.CornerRadius="0,8,8,0">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedLightButton}">
                            <Setter Property="Background" Value="#E0E0E0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource PurpleThemeBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>

                    <TextBlock Text="{Binding ButtonText}" TextWrapping="Wrap" TextAlignment="Center" />
                </Button>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>